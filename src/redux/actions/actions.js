export const email = (type, payload) => {
    return {
        type,
        payload,
    }
}

export const password = (type, payload) => {
    return {
        type,
        payload,
    }
}

export const confirmPassword = (type, payload) => {
    return {
        type,
        payload,
    }
}

export const oldPassword = (type, payload) => {
    return {
        type,
        payload,
    }
}

